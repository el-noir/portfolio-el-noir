name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm

      - name: Setup Pages
        uses: actions/configure-pages@v4
        with:
          static_site_generator: next

      - name: Install dependencies
        run: npm ci

      - name: Build with Next.js
        run: npm run build
      
      - name: Check build output directory
        run: |
          echo "Contents of workspace:"
          ls -la
          echo "Checking for out directory:"
          ls -la out/ || echo "out/ doesn't exist"
          echo "Checking .next directory:"
          ls -la .next/ || echo ".next doesn't exist"
      
      - name: Verify build output
        run: |
          if [ -f "./out/index.html" ]; then
            echo "✓ out/index.html exists"
          elif [ -f "./.next/standalone/index.html" ]; then
            echo "Found in .next/standalone"
          else
            echo "❌ index.html not found in expected locations"
            exit 1
          fi
      
      - name: Add .nojekyll file
        run: touch ./out/.nojekyll
      
      - name: Add CNAME for custom domain
        run: echo "mudasirshah.live" > ./out/CNAME
      
      - name: Verify deployment files
        run: |
          echo "Files in out directory:"
          ls -la ./out/
          echo "CNAME content:"
          cat ./out/CNAME

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./out

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
